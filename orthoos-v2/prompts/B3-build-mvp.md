# B3 — Build the MVP

**Run in:** Claude Code. **Gate:** only after B1 and B2 are signed off.

---

Read CLAUDE.md. Build ONLY the concierge second-opinion MVP described in
Part C7. Reuse everything marked REUSE in the B1 report. Before writing code,
produce: (1) the exact list of files/workflows you will create or change,
(2) the rollback path, (3) the data flow showing where de-identification
happens and where consent is stored. Wait for approval. Then implement,
test with synthetic cases only, verify the Razorpay webhook path with a
test payment, and document. Stop at the first point where a production
workflow must change and ask.
